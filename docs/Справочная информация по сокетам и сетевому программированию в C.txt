#include <sys/socket.h> - заголовочный файл для сокетов
#include <netinet/in.h> - структуры данных для сетей и функции для преобразования данных из сетевого представления в представления для компьютера и обратно
#include <arpa/inet.h> - вспомогательные функции для преобразования

int socket(PF_INET, SOCK_STREAM, 0) - создает TCP-сокет в пространстве IPv4, возвращает файловый дескриптор, который предназначен для работы с сокетом.
int bind(int sockfd, sockaddr* addr, socklen_t socklen)